---
name: video-prompts-v13
description: 视频提示词生成入口（v13.0 规则源·委派执行版）。整章小说视频流程进入视频提示词阶段时使用：本 Skill 自身不含任何创作规则，只负责解析并校验已安装的小家 v13.0 规则源（xiaojia-prompt-generator），把工作流的完整输入原样交给它、把它的唯一完整章节 Markdown 原样接回。规则一律以安装版 skill 原文为执行依据，禁止精简、禁止概括、禁止自创、禁止用总控摘要替代剧本。替代原 video-prompts-v12 入口。
metadata:
  version: "13.0.0-wiring.1"
  rule_source: xiaojia-prompt-generator
  rule_source_version: "13.0"
  supersedes: video-prompts-v12
  edition: 适配层（不含规则正文；规则正文只存在于安装版 skill，禁止复制入库）
---

# 视频提示词入口 · v13.0 规则源委派

## 0. 定位与唯一职责

本 Skill 是**入口与交接层**，不是生成器：

1. 解析并校验规则源（已安装的小家 v13.0 skill）；
2. 把工作流的生产输入**原样**交给规则源子任务；
3. 把规则源产出的**唯一**完整章节视频提示词 Markdown 原样接回。

🔴 本文件内不得出现任何分镜创作规则、句法样本、特效库或质检清单。规则一律从安装版 skill 现场读取；把规则复制进本仓库（或任何本地副本）都视为违规，会导致两套规则漂移而失去"严格按 v13.0 执行"的意义。

## 1. 规则源定位与校验

按 `references/installed-rule-source.md` 的三步顺序解析路径，读取源 SKILL.md 的 `metadata.version`，必须等于 `13.0`（或用户当次明确指定的更高版本）。校验未通过**不得降级**到 v12.6，也不得凭记忆复述规则，直接停机报告。

只读纪律：对规则源只做读取（SKILL.md + 其 `references/` 下被 96 号契约点名的子节）。禁止修改、禁止移入本仓库、禁止改写其措辞、禁止把其内容并入任何中间摘要文件。

## 2. 执行流程（不得跳步）

1. 读取规则源 `SKILL.md` 全文（总控层：双模式、M0–M5、内容类型分流、冲突优先级、输出协议、路由总表）。
2. 按规则源 `references/96-规则执行契约与证据回执.md` 建立本次任务的项目契约、事实账本、缺口账本与**文件+子节依赖清单**。
3. 逐项读取依赖清单里的完整子节（只读标题不算读取完成），包括画质基准原文与内容类型专属规则组。
4. 在规则源的规则下规划分镜、每镜秒数与逐镜正文，按其输出协议落稿（打戏四段式＋正向词袋；文戏按其格式）。
5. 按规则源自身的三层验收与最终收口完成自检（成立硬闸、运镜动机闸、字数与时长闸、跨镜承接）。
6. 只输出唯一完整章节 Markdown 到工作流指定路径；内部状态、规则卡、证据回执、依赖清单一律不入正文。

## 3. 输出与下游合同

沿用原入口的下游合同（细则见 `references/output-and-handoff-contract.md`）：默认命名 `<章节名>_完整视频提示词_v13.0.md`、按 S 编号顺序收录全部镜头、逐镜正文完整不得精简、逐镜本地图号从 `@image1` 连续、Seedance 参考图预算与连续性三分类照旧。制作层（`doubao-video-production`）直接按 S 编号从该 Markdown 提取每镜正文，不经过摘要或二次改写。

## 4. 禁止事项（违反即返工）

- 禁止精简：不得压缩镜头数、时长、正文字数、规则要求的证据项。
- 禁止自创：不得引入规则源以外的写法、模板、字段或"优化版提示词"。
- 禁止替代：不得用总控摘要、资产摘要或图片提示词替代完整正式剧本输入。
- 禁止降档：整章生成默认完整档，篇幅长时继续写入同一 MD，不因篇幅降档。
- 禁止跳读：不得只读规则源 SKILL.md 就落稿，必须按 96 号依赖清单逐项读取。
- 禁止改写上游：不得改动剧本、资产清单、图号约定与用户锁定内容。
- 禁止越权：模型、时长、参考图预算等制作约束由工作流传入，本层不擅自放宽，也不在制作阶段改秒数或改正文。

## 5. 异常处理

| 情形 | 处置 |
|---|---|
| 规则源未安装 / 版本不符 | 停机，报出实际路径与版本，请用户安装或指定规则源路径；不回落 v12.6 |
| 规则源 `references/` 缺件 | 停机并列出缺失文件；不得凭残件生成 |
| 用户当次指定使用 v12.6 入口 | 按用户指令走 `video-prompts-v12`（历史锁定稿沿用旧产物，不自动追溯重写） |
| 制作层要求改秒数或删参考图绑定 | 不改正文，交回本层重规划受影响镜头 |
