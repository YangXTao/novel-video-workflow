# v13.1.1 规则源接线说明（`v13.0-alpha` Codex适配）

## 1. 目标

视频提示词阶段不再在入口内联规则，改为**委派同一工作流包中的小家 v13.1.1 Skill**：

- 用户只需说"做第 N 章（或第 N–M 章）"，总控按依赖顺序调用各 Skill 与 MCP，产出剧本 → 三类图片提示词 → 图片与资产清单 → **完整章节视频提示词** → 逐镜视频。
- 视频提示词的规则来源是独立的 `xiaojia-prompt-generator` Skill，**严格按 v13.1.1 执行，不精简、不自创、不替换**。
- 该规则 Skill 随分支一并交付，但不复制进适配入口；版本与 SHA-256 必须现场校验。

## 2. 为什么必须是委派形态

| | 内联单文件（已废弃形态） | 委派已安装规则源（当前） |
|---|---|---|
| 形态 | 全量规则内联成单个 SKILL.md | 总控 SKILL.md（19 KB）+ `references/` 29 个规则文件 |
| 加载 | 超长必被压缩/截断 → 规则静默丢失、输出被精简 | 渐进式披露：按其 96 号执行契约先建依赖清单，再逐子节读取并留证据 |
| 执行保证 | 无独立契约文件，读到多少算多少 | 96 号执行契约 + 事实账本 + 缺口账本 + 规则卡 + 证据回执，最后读 100 号最终覆盖 |

## 3. 结构

```
skills/video-prompts-v13/                 ← 新增入口（薄适配层，不含规则）
├── SKILL.md                              定位/校验/执行流程/禁止事项/异常处理
├── references/installed-rule-source.md   规则源三步定位、校验项、只读纪律、为什么必须是委派形态
├── references/output-and-handoff-contract.md  交付物命名、逐镜自包含、图号与参考图预算、连续性三分类
├── scripts/verify_rule_source.ps1        校验规则源并输出路径/版本/哈希（JSON 可用）
└── agents/openai.yaml                    入口默认提示词
skills/xiaojia-prompt-generator/          ← v13.1.1 独立规则源（SKILL.md + 29份 references）
skills/storyboard-quota-check/            ← 出稿后 A–I 配额检查
```

> 本分支只保留 `video-prompts-v13` 一个视频提示词入口；内联式入口已彻底移除，工作副本内不并存两套规则。

## 4. 规则源定位顺序

1. 项目根 `novel_video_production_config.json` → `video_prompt_generation.rule_source`（可加 `rule_source_version`）
2. 环境变量 `XIAOJIA_SKILL_DIR`
3. 与 `video-prompts-v13` 同级的 `xiaojia-prompt-generator`
4. `~/.codex/skills/xiaojia-prompt-generator`
5. `~/.workbuddy/skills/xiaojia-prompt-generator`（兼容回退）

同级内置规则源优先，避免旧客户端残留副本掩盖当前分支版本。命中后只使用这一份，不混读其他副本。

校验：`SKILL.md` 存在、`metadata.version` 达标（默认 `13.1.1`）、`references/` 含 29 个现行规则文件、96 号契约文件存在；输出 `rule_sha256` 供 `screenplay_trigger_audit.json` 使用。校验失败**停机**，不回落其他版本。

```powershell
pwsh -NoProfile -File skills/video-prompts-v13/scripts/verify_rule_source.ps1 -Json
```

> 仓库内含中文的 `.ps1` 一律以 UTF-8 **带 BOM** 保存：Windows PowerShell 5.1 会把无 BOM 的脚本按 ANSI 解码，中文乱码并使校验直接失败。实测加 BOM 后 `verify_rule_source.ps1` 在 5.1 下正确输出 `version 13.1.1 / references 29 / rc=0`。

## 5. 已同步的耦合点

| 文件 | 改动 |
|---|---|
| `skills/novel-video-orchestrator/SKILL.md` | 调用顺序、输入直交、附加创作要求、接口说明、交付命名 → v13 入口 / `_完整视频提示词.md`（要时间戳时追加 `_<YYYYMMDD_HHMM>`） |
| `.../references/workflow.md` | 固定调用模板 `使用 $video-prompts-v13`、输入清单与交付说明中的版本标识 |
| `.../references/state-contract.md` | `v10_prompts` 阶段的默认文件名 |
| `.../references/duration-and-model-planning.md` | 规划权归属、命名、重拆回退对象 |
| `.../references/isolated-creative-tasks.md` | 独立上下文子任务适配的 Skill 列表 |
| `skills/novel-to-screenplay/SKILL.md`、`references/v13-trigger-bridge.md`、`scripts/test_screenplay_validator.ps1` | 触发桥接改读 v13 入口 + 规则源哈希；测试脚本改为调用 `verify_rule_source.ps1` 动态解析路径与版本 |
| `skills/character-image-prompts` / `scene-image-prompts` / `prop-image-prompts` | 下游规划方指向 v13 入口 |
| `skills/doubao-video-production`（SKILL + error-recovery-log + resolve_shot_bindings.ps1） | 超预算回交对象、恢复路径、错误提示 |
| `README.md`、`SNAPSHOT.json` | 分支说明、目录表、快照哈希 |

## 6. 不变项（刻意保持）

- 总控**不预写分镜、不锁秒数、不扫描资产目录**；分镜与秒数仍由视频提示词规则源独立规划。
- 完整正式剧本、已有设定、真实资产清单、时长/模型要求、项目级 `additional_directive` 仍是"完整输入直交"。
- 图片阶段仍不等 `scene_shot_map.json`；资产绑定仍由豆包阶段按真实上传顺序完成。
- 历史锁定章节沿用其既有产物，不自动追溯重写；新章节一律走 v13 入口，工作副本内不并存两套规则。

## 7. 使用方式

新章节无需任何额外命令，仍是一句话：

```text
使用 $novel-video-orchestrator 制作第 12 章的视频
```

总控会自动走到视频提示词阶段并调用 v13 入口；若需指定其他章节范围、时长口径或模型，按原模板传入即可。规则源路径不在默认位置时，优先写进项目配置，而不是改 Skill。

## 8. 出稿后强制配额自检（工作流侧，不动规则源）

规则源只保证「规则怎么读、怎么落稿」，**不负责在出稿后回头数一遍**。因此本工作流在交付侧加了一道机械自检：

| 项 | 内容 |
|---|---|
| 合同 | `skills/video-prompts-v13/references/quota-selfcheck.md` |
| 脚本 | `skills/storyboard-quota-check/scripts/quota_check.py`（随工作流一并交付的独立 Skill） |
| 定位顺序 | 项目配置 → 环境变量 → 同级内置 Skill → `~/.codex/skills/` → WorkBuddy兼容回退；调用时显式传入本次校验通过的 `--rules` |
| 触发器 | 视频提示词阶段**出稿后必跑**；硬项非空不得交付，改完须重跑 |
| 自检段 | A 章节级 / B 逐镜配额 / C 证据项密度 / D 硬软项分档 / E 运镜标签越界 / **F 锁死画质原文覆盖率**（F 为信息项，不做通过否决） |
| 交付说明必带 | ①规则源路径+版本+SHA256 ②实际读取的 `references/` 文件与子节清单 ③A–I 九段结果原样。三项只进交付说明，不进正文、不落盘 |

其中 **F 段**专治"没读就编"：把锁死画质原文（打戏 11 号 2.1 / 文戏 20 号 3.1）切成特征表述，统计①段字面命中率。第56章稿实测：v13.0 规则源下 **87%**（27–28/31），v13.1.1 规则源下 **97%**（32/33；唯一未命中项 `Kodak胶片颗粒`，因新版把该词合并为单一特征表述，①段里插了胶片型号即不命中）；未命中的 4 项是调色板后半句那几项自检要求，正文按规则压成了散文——**字面未命中 ≠ 违规**，未命中清单的用途是**指出该人工核语义的位置**。

已接入的耦合点（共 7 个文件）：

- `video-prompts-v13/SKILL.md`：执行流程新增第 7 步；输出合同与异常处理补条目；版本升 `13.1.1-wiring.1`
- `video-prompts-v13/references/output-and-handoff-contract.md`：交接检查增加「附自检结果」项 + 配额自检门禁
- `video-prompts-v13/references/quota-selfcheck.md`：**新增**，自检合同全文
- `novel-video-orchestrator/SKILL.md`：外围交接检查与唯一交付两条各补门禁
- `novel-video-orchestrator/references/workflow.md`：`v10_prompts` 阶段完成条件 + 固定调用模板新增「出稿后自检」行
- `novel-video-orchestrator/references/isolated-creative-tasks.md`：独立创作子任务的交付要求
- `novel-video-orchestrator/references/state-contract.md`：`v10_prompts` 阶段完成条件

**两道防线**：本层出稿后自己跑并把结果附在交付说明（流程契约）；总控接回后**独立重跑同一脚本**作门禁，两次结果应一致（机械校验，不是逐拍内容审计）。

**自检管不到的**：运镜动机闸、戏成立终审、剧本逐句映射、画质词是否真为 11 号 2.1 原文、正文是否泄漏内部口径——这些仍由本层按规则源 70 号与 96 号完成。自检通过不等于内容合规。

## 9. 规则源升级到 v13.1.1（2026-09-16）

规则源由 v13.0 **整体替换**为 **v13.1.1**（多文件树状版；安装方式＝整个 skill 目录替换，v13.1.1 起已无"单文件母版"形态）。
本次工作流改动 **只适配、不改契约**：输入清单、图号预算、连续性三分类、六条禁令、只读委派架构全部不动。

### 9.1 规则源侧的实质变化（影响执行）

| 项 | v13.0 | v13.1.1 |
|---|---|---|
| 打戏画质基准位置 | 10 号 2.1 | **11 号 2.1**（100 号 21.0.0 第 11 条：一切「10 号 2.1」字样为历史误引） |
| 锁死原文里的占位符 | `Kodak胶片颗粒（型号按题材选）`、`[X]%` | 已删净 → **`Kodak胶片颗粒`**；10 号另加「正文只写『Kodak胶片颗粒』不写出具体百分比」 |
| 字数预算 | 「全稿≤5000 硬顶 5500 / 10s≤1500」 | **公式制**：①250＋②400＋③每拍上限×拍数＋④300＋⑤正向词100，**总上限＝公式求和值**；旧参考值经边界值验算废止 |
| 时间参数 | 部分允许保留 | **全面内部化**（铁律58 含"秒数"）：顿帧/升格/快门/帧率/秒数一律写画面词 |
| 承接落字 | 10 号 1.5 有「【上一镜尾帧衔接】」字段名 | 六项状态＝内部账本，**字段标签不进正文**；落字唯一形态＝37 号 17.1 承接拍（自然句织入） |
| 文戏画质复读 | 「本场首镜声明一次、后续只写变化项」 | **每镜完整出现**，压缩为「3.1 原文一句＋本场调色板一句」 |
| 计数类旧闸 | 41 号 6.4 三闸、01 号特写配额、20 号「每镜≥3维」 | 全部废止 → 统一走铁律52 动机驱动 |
| EWS 人物占比 | 12 号 2.6.1「≤8%」 | **≤5%**（统一到 30 号 4.0.2） |
| 玄幻爆炸术语 | 蘑菇云 | **能量蘑菇柱**（禁现代橙黄火球/现代战争蘑菇云） |
| 运镜标签清单 | 242 个 | **235 个**（运镜目标族 44 个不变） |

### 9.2 工作流侧的适配改动

- **版本标识**：入口/合同/提示词/脚本门槛/文档 `13.0` → `13.1.1`；入口 skill 自身版本 `13.0.0-wiring.2` → `13.1.1-wiring.1`
- **交付物命名与规则版本解耦（用户裁决）**：`<章节名>_完整视频提示词_v13.0.md` → **`<章节名>_完整视频提示词.md`**；需要时间戳或多版本对比时追加 `_<YYYYMMDD_HHMM>`。理由：命名里绑规则版本会导致每升一次规则就得改多处工作流文件，且历史产物命名割裂
- **检查脚本升 v3.6**：F 段来源改 11 号 2.1（行匹配，非围栏块）；E 段改为按①段画质基准**分轨**判（打戏镜必须绑视觉目标＝硬项、文戏镜不该有＝软项）；A 段新增铁律54 公式制字数预算三列
- **分支名保持 `v13.0-rules-wiring`**（历史命名；未改名以免破坏既有引用）

### 9.3 已知口径坑（本层执行时必须注意）

1. **打戏镜必须有 `[视觉目标]` 标签、文戏镜不该有**。规则源实测：10 号 64 处视觉目标类方括号、**20 号 0 处**，20 号标杆示例明文"不贴标签"。第57章稿正是在这一点上漏了 3 镜打戏（S11/S13/S14）。
2. **胶片颗粒% 禁入正文**（铁律58 L544 明文列举）——写 `Kodak胶片颗粒` 即可，不得带型号或百分比。文戏 20 号 3.1 在 v13.1.1 里也已删掉「（日常45%）」。
3. **①段 ≤250 与铁律1 锁死原文的冲突在 v13.1.1 里仍无解**（原文本身约 190–270 字，加铁律17 五色调板必然超出）。按用户裁决：**保锁死原文、按软项逐镜披露**，不硬压。
4. **v13.1.1 在三处打戏句法上相对 v13.0 有回退**（铁律8 节奏曲线、12 号 2.6.1 蓄力/对峙运镜、70 号维度3 张力铺垫），其中铁律8 与铁律35/55 构成硬矛盾。按用户裁决：**全部按 v13.1.1 原样执行**，工作流侧不加覆盖条款。

## 10. 特效落实指令（2026-09-16）

### 10.1 为什么加

成稿"特效一般般"不是玄学，是规则源里的一整套**特效硬闸没有落地**——而它们此前**不在工作流的任何委派指令或验收清单里**：调用模板"特效"二字零出现、`additional_directive` 默认空、交接检查只查总时长/允许秒数/各档数量上限/模型比例/对白归属。

实测（第57章 v13.1.1 稿，11 镜 / 125s，4 个打戏镜）：

| 项 | 实测 | 规则源原文 |
|---|---|---|
| 时间手法（顿帧/升格/甩镜/爆闪） | **合计 0 次** | 铁律11 · 12 号 2.15 |
| ⑨EWS再证（暴拉EWS/阶梯拉远） | **0/4** | 铁律19："大特效后接一记暴拉EWS或阶梯拉远…**缺项即补**" |
| 巨物感三连 | 2 个候选巨物镜**都缺②逼近下降、③暴拉超远景** | 铁律33："必走三拍，**缺一不可**" |
| 缺省路由补充特效 | **未见补充层** | 12 号 2.9.1 分支C："用户没提特效也要自动补 1 个匹配补充特效" |
| 铁律5 每击必爆量级词 | 0 镜（该章打戏镜确无攻击命中，受"仅适用存在攻击动作的当拍"边界约束，**属正常**） | 铁律5 |

对照第56章（14 镜全打戏）：时间手法 5 次、EWS 3/14、爆点量级 5 镜——同类项确实随执行波动，说明不是章节题材决定的。

### 10.2 改了什么（三处，全部指向规则源原文，不新增创作规则）

| 位置 | 改动 |
|---|---|
| `novel-video-orchestrator/references/workflow.md` 固定调用模板 | 新增 **「特效落实：」行**——显式点名 12 号 2.9.1 分支C、铁律19 尺度四件套与九要素（含⑨）、铁律33 巨物感三连、铁律38 同屏多特效三色、铁律5 每击必爆与铁律11 链式因果；并声明"本行是指向规则源原文的必做项清单，**不是本模板新增的创作规则**" |
| `video-prompts-v13/references/output-and-handoff-contract.md` | 新增 **§6 特效落实验收**（验收项 / 规则源出处 / 性质 三列对照表） |
| `storyboard-quota-check` 脚本 + SKILL | 脚本新增 **I 段「特效落实」**（v3.7）；SKILL 同步 3.7；段数表述 A–H → **A–I**（工作流侧同步改） |

### 10.3 I 段判据与边界

四组**字面证据**（只扫③段时间轴）：**尺度四件套**（占比/量级/参照/人占）、**时间手法**（顿/升/甩/闪）、**巨物感三连**（仅"打戏镜且标题含巨物词"时判）、**爆点量级与并行特效类数**。

**整段为信息项，不计入硬项/软项**，三个原因：①"什么算大特效镜/巨物镜"须人工判断（脚本用"打戏镜 + 标题含巨物词"做候选，会把叙事里提到巨兽的镜也纳进来）；②铁律5 有适用边界，爆点量级为 0 **不等于**违规；③时间手法与 EWS 的**位置正确性**（⑨是否真接在大特效之后）脚本判不了。

**用法**：看"某项全稿普遍为 ·"。全稿普遍为 · 时，优先怀疑**该镜根本没读到对应规则子节**，连同镜号交回执行层核对——这是本段的主要价值：把"特效一般般"变成可定位的镜号与缺失项。

**实现坑（本轮实际踩到并修）**：巨物镜判定必须用镜标题原文（脚本新存 `title` 字段）；`tag` 只保留"｜"之前的镜号，用它判会**恒不命中**，导致"巨物镜 0 个"的假结论。

## 11. 强度诉求的传参（2026-09-16）

### 11.1 一次事故：传参整行被漏掉

手写第57章子任务指令时，**漏掉了固定模板的「项目级附加创作要求」行** —— 项目配置里已有（2026-09-13 确认）的强度诉求因此没传下去。

对照两章的自检 I 段：

| | 第56章（指令里逐条点名了特效手法与元素库来源） | 第57章（漏掉传参行） |
|---|---|---|
| 时间手法（顿/升/甩/闪） | 5 次 | **0** |
| 铁律19⑨ EWS再证 | 3/14 | **0/4** |
| 铁律5 爆点量级 | 5 镜 | **0 镜** |

两章题材不同、数字不能直接比大小；但"**某项全稿为 0**"是结构性缺失。这说明：**规则源的特效硬闸（本轮新接的三处）解决"知不知道要做什么"，强度诉求解决"做多足"——两者缺一不可。**

**修复**：
1. 模板**逐行不得省略**（内容为空也要显式写"无"）；
2. 新增「**强度与特效诉求**」行，承接用户当次的**口语**诉求，**逐字传入、不得概括** —— 口语强度词本身就是档位指定（铁律19"用户/剧本数值最高优先"、铁律0.5-D 用户指定优先于默认档），概括成"高强度表现"会丢掉档位信息。

### 11.2 写法指南

见 → `docs/additional-directive-guide.md`：口语强度词 → 规则源条款的映射表（"特效拉满"=铁律19 禁退回平常档、"拖尾"=12 号 2.9.1 ⑨、"持续时长长的特效"=生命周期必写…），以及一份**可直接复制进配置的建议文本**。两条红线：不得凭空新增剧本没有的法阵/法相/领域（铁律48）；不得写工程参数（铁律58）。
