# v12.9-alpha 更新说明

## 2026-09-11

- 视频提示词生成器接入小家 V12.9 多文件规则，调用名保留 `video-prompts-v12`，版本 `12.9.0`；移除活动目录中的 V12.6 母版，旧版仍可从 Git 历史及本地备份恢复。
- 字数口径沿用已修订的单镜说明；未修改 V12.9 原秒数规则。
- 总控固定交接完整剧本、真实资产与已有设定、用户制作要求、锁定内容及输出文件；由生成器读取适用 references 并独立创作，总控不预分镜或二次压缩。
- 加入完整执行、防摘要与防“同上”省略要求；全部 S 镜头顺序交付同一 Markdown。
- 清除剧本与视频生产入口中的 v12.4 旧执行指令，改为当前 Skill 引用；锁定历史稿仍沿用。
- 尾帧由执行者实际查看和审核，不默认等待用户签收。登记脚本新增 `-QaReviewer` 与 `-QaBasis`，缺失时拒绝写入，不再固定声称“经人工确认”；调用说明同步更新。
- 旧阅读审计与封存工具保留用于专项诊断，明确不属于正常生产步骤。
- 保留现有 MCP、浏览器工具、账号额度分流、文字回复恢复及扩展无水印下载规则；本次不上传账号、Cookie、浏览器持久化目录或章节媒体。

## 验证范围

尾帧脚本已用临时数据验证：缺少审核信息时拒绝且清单不变，提供信息后如实保存并登记依赖；六个修正文件在安装、工作及仓库副本间哈希一致。本次未执行浏览器或整章生产回归，Alpha 不代表端到端无故障保证。
