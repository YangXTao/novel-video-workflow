# v12.6-stable 更新说明

发布日期：2026-09-10。基于 video-prompts-v12.6 分支的当前工作流快照；本次发布不包含小说正文、生成媒体、账号清单、Cookie、登录数据或浏览器用户目录。不新增 tag，版本入口为分支 v12.6-stable。

## 创作与总控

- 视频提示词入口改为完整 v12.6 母版直接作为 SKILL.md；调用名仍为 video-prompts-v12。母版创作规则不因本轮交接修复而改写。
- 同步此前移除的拆分规则及旧外部视频审计脚本。references/runtime-v12.6-full.md 为保留的历史兼容副本，当前视频创作和剧本桥接不以它作为活动来源。
- 总控只传递完整剧本、既有设定、真实资产、用户时长/模型/比例要求及输出路径；不预写分镜、不另行精简。
- 整章生成固定指定：完整正式剧本、生成模式、完整档、不因篇幅降档。V12.6自行规划镜头与时长；用户明确指定的其他任务模式优先。
- 新创作使用逐镜连续图号约定，不把全章资产序号直接当上传序号；历史正文不自动重编号。完整章节MD为视频提示词正式交付，时间戳仅在明确要求时使用。
- 人物、场景、道具独立接收完整输入并原样交付；人物三视图、场景与道具单画面保持现行模板。

## 资产与提交

- 资产校验新增 AssetsOnly/Production 两阶段，默认仍为 Production以兼容旧调用。生图后不要求尚未规划的S镜头；视频制作接到完整MD后建立镜头资产与尾帧关联。
- resolve_shot_bindings.ps1 新增 PromptText 参数，结合镜头 body_reference_bindings 将正文图号绑定到真实资产ID；尾帧不挤占已有角色编号。
- JSON保留完整上传顺序，并提供 supplemental_legend；正式调用的标准输出只包含补充映射，没有补充则为空。未传PromptText的旧调用保留旧输出兼容，但不能直接用于正式提交。
- 正文不改；补充真实映射位于执行指令之后、完整正文之前，不重复正文已有映射。缺图、冲突、无法填满的编号或超出10张时拦截，不用无关图凑数。映射身份仍需要执行者依据正文和资产事实确认，脚本不能识别人物长相。
- 执行指令统一为：严格执行下方第N章Sxx提示词，不能改写或摘要！！！直接生成视频；完成后返回原始视频卡片及结果链接。
- 法术/打戏或用户指定无BGM的镜头，运行时在正文外追加一次“禁止背景音乐！！！”，已有同句不重复，环境音和对白不删除；冲突先解决，不覆盖创作MD。

## 恢复与下载

- 对话入口通用文字恢复默认执行，不再要求项目开关；账号特有额外操作仍由明确配置/用户说明决定。总控不再复制“一次恢复就停”的旧规则。
- 确认请求走同会话完整重传、必要时新豆包对话；其他纯文字走重新生成及原消息原样重提。按现行4条独立完成纯文字回复门槛退出；云电脑保持独立三轮判据。未知任务先核查，不盲目重提。
- 正式下载优先且默认只允许右下角扩展“无水印资源”；URL中lr=unwatermarked不是无水印证明，须同时登记来源与实际画面检查。替代下载方式需用户明确授权。
- 包含此前本地图片MCP/辅助脚本变更：按需连接独立Chrome，下载完成不自动关闭结果页；本次只归档现有代码，没有改动运行中浏览器或登录目录。

## 剧本桥接

- 当前规则来源统一为 video-prompts-v12/SKILL.md，版本从metadata.version读取，哈希取该文件；不再在桥接说明中使用12.4或旧规则包校验结果。
- 校验器支持读取SKILL.md版本；历史规则包分支保留兼容，不用于当前流程。

## 发布前验证

- 8个Skill的结构检查通过。
- 16个PowerShell脚本和19个JavaScript文件的语法检查通过。
- test_submission_bindings.ps1通过：两阶段资产检查、正文编号保留、尾帧、补充映射、正文不变、重复/稀疏/缺失绑定拦截、状态及数量限制、空补充、无图号输入、标准输出与旧调用兼容。
- test_screenplay_validator.ps1的10项回归通过；test_creative_task_io.ps1的输入、读取范围、封存及篡改检查通过。后者是开发测试，不是每章新增创作审计。
- 仓库Skill文件与安装、开发副本逐文件哈希一致。发布清单SNAPSHOT.json重新生成，记录当前版本文件而不是已移除的旧规则。
- 敏感运行文件名及常见私钥/访问令牌模式扫描未发现匹配；这不是无限范围的安全保证。
- 母版内原有尾随空格原样保留，不为格式检查改动母版。

## 验证边界

stable表示此次静态检查与本地回归通过，不保证模型永不漏写，也不代表所有网页行为已重新实测。本次未提交ChatGPT生图/豆包视频、未消耗生成额度、未测试登录持久化或平台额度。MCP/Chrome仍需按部署环境配置；克隆仓库不会恢复原电脑登录状态。已有正文编号不可用时安全阻塞，需要解决真实绑定或获得修订授权，不会自动改正文。
